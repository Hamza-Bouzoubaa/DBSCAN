import java.io.FileNotFoundException;
import java.util.ArrayList;
import java.util.List;



public class Main {
    public static void main(String[] args) throws FileNotFoundException {

        /*
        List db = new ArrayList();


        String file_name = "Point_cloud_2";
        db=(DBScan.read(file_name));  // Reading file and creating a List of Point3D
        DBScan scan;
        scan = new DBScan(db);                              // Creating a new DBscan object
        //scan.setEps(0.4);                                    //setting Eps
        //scan.setMinPts(20);                                 // Setting MinPts
        double e = scan.setEps(1);
        double p = scan.setMinPts(10);
        scan.findCluster();                                  // Finding clusters
        List l = scan.clusterSize(db);
        int sum=0;

        for (int i = 0; i < l.size(); i++) {
            System.out.println(l.get(i));

        }

        scan.save(file_name+"clusters_"+ e +"_"+ p +"_"+ scan.getNumberOfClusters());                       // Saving in new CSV file
        */


    }
}